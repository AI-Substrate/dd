# Execution log — Phase 3: Package & release readiness (ph-356b)

**Plan**: `docs/plans/001-dd-extraction/plan.dd.json` · **Tasks**: `tasks.dd.json`
**Dispatch**: dlg-0003, **PARTIAL** — tk-0003, tk-0004, tk-0006, then tk-0005 folded in by a
mid-run scope amendment. **tk-0001 and tk-0002 deliberately untouched** (see § Held).
**Upstream basis**: `d08f4942` (read-only, unchanged).

---

## Held, not done

| id | state | why |
|---|---|---|
| tk-0001 | `unchecked` | Jordan's OQ-1/OQ-2 rulings have not landed. A hard precondition. |
| tk-0002 | `unchecked` | The exports FREEZE depends on tk-0001. |

Nothing in this phase infers, guesses, provisionally chooses or partially prepares an exports
shape. `./plan` remains absent from the public map, and `test/package-manifest.test.ts` asserts
**nothing** about `exports` — a partial expectation there would have decided the held question by
accident. The three delivered tasks were confirmed by execution to have **no coupling** to the
freeze: the pack gate keys on the `files` map and the tarball's file LIST, which `exports` does
not affect.

---

## tk-0003 — the rename (RULED by o-prime 2026-08-07)

Commit `13d86f8` `feat(cli)!: the binary is named dd, and now says so`.

**The count reconciles exactly.** 65 occurrences of `harness dd` existed in `src/`. Classified
before editing:

| Surface | Count | Action |
|---|---|---|
| act `next_action` + human-mode output strings | 42 | renamed |
| `RENDER_BANNER` | 1 | renamed |
| **the ruled 43** | **43** | — |
| baked docs content (`src/docs/content/*.md`) | 12 | renamed (the third named surface) |
| `dd-docs-manifest.json` `$comment`, `graph.ts` usage example | 2 | renamed — they describe THIS package |
| upstream-provenance comments | 6 | **kept** |
| **total** | **65** | |

The 43 matching the ruling with comments excluded is the evidence that comments were out of
scope. The six kept are `app.ts` ("upstream nests them beneath `harness dd …`, but here the
binary IS `dd`"), `acts/status.ts` (the port ledger naming the verbs' upstream home) and the four
`E4xx` block headers citing their plan-065 allocation. Each is a true statement ABOUT upstream;
renaming them would have made them false.

**Zero stale banners repo-wide** — which is the invariant that matters, and is the one actually
achieved. The rename commit `13d86f8` regenerated **11 of the 14** generated siblings; the other 3
(`phase-1`, `phase-2` and `phase-4` `tasks.dd.md`) already carried the new banner from parent
`1773470`, o-prime having built them against a local bin that already held the rename.

An earlier version of this log claimed all 14 moved in the same commit. That was a
receipt-accuracy defect, not a functional one — `git diff-tree` does not support it, and it is
corrected here rather than by rewriting history. Measured at `8a13e53`:

```bash
git ls-files | grep -E '\.(dd|expected)\.md$' | wc -l                 # 14 generated siblings
git diff-tree -r --no-commit-id --name-only 13d86f8 \
  | grep -cE '\.(dd|expected)\.md$'                                   # 11 moved in the rename commit
git ls-files | grep -E '\.(dd|expected)\.md$' \
  | xargs grep -L 'GENERATED by `dd build`'                           # (empty) — no stale banner
```

The last command is the invariant, and is deliberately phrased as "which generated sibling LACKS
the new banner" rather than "which file contains the old string": `assets/handover-notes.md`
quotes the old banner verbatim as documentation for koala's swap, and a naive `grep` for the old
string counts that prose as a defect.

The goldens are not regenerated by a switch — `dd build` ships no `--update-goldens` hatch and a
test pins that absence — they are proven by the renderer suite's byte-for-byte comparison, which
passed.

`drift.dd.md` kept its banner updated too, so the ONLY difference from `drift.expected.md` remains
the deliberate hand-edit the fixture exists to represent.

**Guard**: `test/acts/binary-name.test.ts` (16 rows), two halves — a source half pinning per-file
counts against an allow-list and requiring every survivor to sit on a comment line, and a runtime
half driving the shipped bin over 12 invocations in **both** output modes. Mutation-tested:
reverting one `next_action` reddened both halves; restoring it returned 16/16.

## tk-0004 — prepack

Commit `b24aa76`. `prepack: npm run build`, so `npm pack` from a fresh clone self-builds.

`files` was already correct; what was missing was anything that would notice if it stopped being.
`test/package-manifest.test.ts` (10 rows) reads real `npm pack --dry-run` output and asserts the
closed top-level set, zero leakage, the execute bit on `bin/dd.js`, the two runtime deps, and
`package.json` version == `.release-please-manifest.json`. Verified beyond dry-run: packed,
installed `--omit=dev` into a clean temp project, drove the installed bin through its symlink.

**No publish, no tags, no releases.**

## tk-0005 — the pack gate (released mid-run by o-prime)

Commit `dcf6f1e`. `scripts/pack-gate.sh`, wired as `just pack-gate` and a CI step — one script, so
CI and local cannot drift into testing different things. Deliberately NOT in `just checks`: it
clones and installs, ~1 minute, the wrong shape for the inner loop.

Seven steps, all passing: clean clone of HEAD (asserting no `dist/`, no `node_modules/`, so
`prepack` cannot be credited for someone else's work) → `npm ci` → `npm pack` → file-list
assertion → `--omit=dev` install → the installed bin against a fixture corpus → every claimed verb
answers `--help`.

The jiti proof runs **through the tarball**: an adapter in untranspiled TypeScript turns `2610`
into `**43h 30m**` in the rendered markdown, which is only possible if the shipped `jiti` did the
transpiling. dw-0005 asks for an assertion that fails when the fixture is absent, so the gate runs
that control itself — same document, adapter removed, `2610` must survive.

**The gate was mutation-tested twice**, because a gate that cannot fail is decoration:

| mutation (temp commit, reverted) | result |
|---|---|
| `prepack` removed from `package.json` | `pack-gate FAILED: prepack did not build dist/` |
| a `dist/` artifact committed | `pack-gate FAILED: the clone already has dist/`, **exit code 1** |

## tk-0006 — docs

Commit `d57b31f`. Written after the rename, so every example shows `dd`; a test asserts the README
contains no `harness dd`.

`README.md` — install, quick start, verb tour, envelope contract + exit map, addresses, completion
states, the four-root resolution ladder in precedence order, custom render types, development
commands. It now ships in the tarball.

`docs/how/dd/` — both baked entries as verbatim ports under a header naming their source, plus an
index. A test asserts each copy is exactly `header + unchanged source`, so "verbatim" is checked.

`test/docs-surface.test.ts` (9 rows) is the anti-rot instrument: it **extracts the README's own
quick-start heredocs** and runs them through the shipped bin — validate, build, get, set, and a
refused write that leaves the file byte-identical (the README's own claim about `dd set`). Nothing
is retyped into the test, because a retyped copy is what drifts.

---

## Discoveries & Learnings

| # | Tag | What |
|---|---|---|
| 1 | **Noteworthy** | **The banner is a wire format and the two renderers now disagree.** Measured precisely rather than assumed. `harness dd set/add/rm/build` **silently** rewrites the banner back while reporting `ok`; `harness dd build --check` false-fails `E422`. `harness dd validate` and `harness plan validate` are **unaffected** (degraded, exit 0, 0 errors, zero drift-class findings). That last measurement is why the split stayed narrow — it corrected an overstatement in the escalation chain. Ruled into the **RENDERER AUTHORITY SPLIT** guardrail (`plan.dd.md`, commit `1773470`). Recorded for handover in `assets/handover-notes.md`. |
| 2 | **Noteworthy** | **A silent-failure mechanism is never "informational".** The regression above was first escalated under that label; the facts were right, the triage was not, and it nearly buried the highest-severity item in the message. Severity belongs to the failure MODE, not to the confidence level. |
| 3 | **Deferred** | **CI's `package-smoke` job had been asserting a falsehood since `713a43d`** — `dd status` must answer `unconfigured`/exit 2. True while verbs were landing; the ledger flipped when the tenth registered. An escape from phase 2 (which updated the vitest smoke tests but not the workflow). Fixed here to assert the honest post-port truth; verified against a real installed tarball. |
| 4 | **Deferred** | **The baked overview points at `docs/how/dd/exemplar/`, which is not ported.** Verbatim upstream text describing files that live upstream. Not rewritten unilaterally — the rename ruling covered command strings, not claims about what ships. `docs/how/dd/README.md` names the gap and lists the runnable equivalents that DO exist. Needs a ruling: port the corpus, or amend the source paragraph. |
| 5 | Noteworthy | **`git clone --local` is refused on a shallow repository**, which is exactly what `actions/checkout` produces. The gate asks `--is-shallow-repository` first rather than cloning through a warning. Caught by testing the CI shape locally, not by CI. |
| 6 | Noteworthy | **`the-flow.json`/`the-flow.md` are forbidden paths and were dirty with o-prime's edits.** `git add -A` swept them into the stage twice; both times unstaged before commit. A blanket add is unsafe in a two-writer repo — check `git status` against the forbidden list before every commit. |
| 7 | Noteworthy | Shortening `harness dd …` to `dd …` reflowed several string literals under the print width, so `biome` failed `lint` on formatting twice. Mechanical, `just fix` resolves it — but a rename that changes string LENGTH is a formatting change too. |

---

## Phase gate

Per the **MEASURED-AT STAMPING** guardrail (`plan.dd.md`, o-prime `b479372`), every count below
carries the commit it was measured at and the command that re-derives it. A count without its SHA
is an assertion, not a measurement.

**Measured at `c0af550`** (the only later commit is this log entry itself).

| Gate | Command | Result |
|---|---|---|
| Repo lane | `just checks` | **exit 0** — 680 tests, 58 files |
| Artifact | `bash scripts/pack-gate.sh` | **exit 0** — PASSED |
| Semantic authority | `harness plan validate docs/plans/001-dd-extraction/plan.dd.json --address "docs/plans/001-dd-extraction/assets/tasks/phase-3/tasks.dd.json#tasks"` | **exit 0**, `degraded`, **0 ERROR**, **15 WARN** |
| Drift authority | local `node bin/dd.js build --check <doc>` × 6 documents | **exit 0**, `drift: false` on all |

The 15 WARNs are 0 errors and expected: **6 `contradiction`** rows (task `checked`, its AC still
`unchecked` — **AC flips are o-prime's**, `plan.dd.json` is outside this scope) and **9
`open-completable`** rows, which include the correctly-held tk-0001/tk-0002.

This count is expected to move without anything being wrong: an earlier revision of this log
recorded **11 WARN**, measured before o-prime authored the phase-4 tasks underneath this phase.
The plan is alive, so the number is a reading, not a property — which is exactly why it is stamped
rather than merely corrected. Re-derive with the semantic-authority command above and compare
`data.counts`, not this table.

**Tasks**: tk-0003 ✅ · tk-0004 ✅ · tk-0005 ✅ · tk-0006 ✅ · tk-0001 held · tk-0002 held.
**Done-when**: dw-0003, dw-0004, dw-0005, dw-0006 all `checked`.

---

## Fix round — review dlg-0003 `FIX_REQUIRED` (4 items)

Review: `pij-blind-deer`, `dlg-0003-review.md`. Four items, one HIGH. The exports-freeze hold was
confirmed correct and is untouched — tk-0001/tk-0002 remain `unchecked`.

### 1 · HIGH — the README anti-rot guard was vacuous (`8a13e53`)

`test/docs-surface.test.ts` claimed to run the README's quick start through the shipped bin. It
extracted the two JSON heredocs and then **retyped the four `dd` invocations as literal arrays**.
The reviewer changed a quick-start verb to `dd nonexistent-verb` and the guard stayed **9/9
green** — a README documenting a command the binary does not have passed the guard whose entire
purpose is to catch that. My phase-3 receipt claimed the commands were extracted. The heredoc half
was true; the command half was not.

The quick start is now parsed into a transcript (`mkdir`, heredoc writes, `dd …` lines) and
replayed step by step. The commands are the README's own bytes, and so are the expectations: the
rendered sibling is checked against the title and claims of the README's own document, `dd get`
against the state that document declares, `dd set` against the value the README's own command
passes. Piped output auto-selects JSON, so the literal documented line answers an envelope — no
flag is added to make it testable.

The exit code is judged **before** any parsing, so a bad command fails **as a bad command**, and
steps are recorded rather than thrown at, so one broken line yields one failure that names it
instead of a cascade that buries its cause.

**Mutation record — acceptance evidence, run at `c0af550`:**

| # | Mutation | Result | Failing assertion |
|---|---|---|---|
| baseline | none | **GREEN** 12/12 | — |
| M1 | quick-start verb → `dd nonexistent-verb` (the reviewer's exact mutation) | **RED**, 1 row | ``` `dd nonexistent-verb review.dd.json` exited 1: error: unknown command 'nonexistent-verb' ``` |
| — | M1 restored | **GREEN** 12/12, README byte-identical to HEAD | — |
| M2 | real verb, address the document does not contain (`dw-4e01` → `dw-9999`) | **RED** | ``` `dd get …#items/dw-9999/state` exited 1 ``` (E450) |
| M3 | an `rm -rf` line added to the quick start | **RED at collection** | `README quick start has a line this guard cannot execute: rm -rf review.dd.json` |

M1 is the reviewer's reproduction, and it now reds **on the command's exit code**. M2 shows the
guard is not merely a spell-checker for verb names. M3 shows an unrecognised line is fatal rather
than skipped — silently ignoring the unparseable would be the same vacuity in a new costume.

### 2 · MEDIUM — the same-commit claim (`91b2103`)

Corrected in the record, not by rewriting history. See the rename section above: 11 of 14 moved in
`13d86f8`, 3 already carried the new banner from `1773470`, and the invariant that holds is zero
stale banners repo-wide.

### 3 · MEDIUM — stale count, and the recording pattern (`91b2103`)

11 WARN → 15, and every count in this log now carries the SHA it was measured at plus the command
that re-derives it, per **MEASURED-AT STAMPING** (`plan.dd.md`, o-prime `b479372`). Also folded in
the `b479372` correction that `harness plan validate` is not drift-affected, so the
harness-drift adjudication clause in `handover-notes.md` describes a case that cannot arise.

### 4 · MEDIUM — the `dd-overview` exemplar pointer (`0a2f1c3`)

Amended, not ported, per the ruling. The deeper reference pages and the `exemplar/` corpus are
marked as living upstream; the paragraph names the three runnable equivalents that ship here. The
refusal reason is recorded in `docs/how/dd/README.md` so it is not re-litigated: the corpus carries
`meta.certainty: Partial`, the contested value of dd-next #10, and it is the file new authors copy.

### Fix-round gate — measured at `c0af550`

| Gate | Result |
|---|---|
| `just checks` | **exit 0** — 680 tests, 58 files |
| `bash scripts/pack-gate.sh` | **exit 0** — PASSED |
| `harness plan validate … #tasks` | **exit 0**, `degraded`, **0 ERROR**, 15 WARN |
| local `dd build --check` × 6 | **exit 0**, `drift: false` on all |

### Discovery

| # | Tag | Finding |
|---|---|---|
| 8 | **Escaped defect class** | The vacuous guard and the stale CI ledger assertion are the same failure: a test whose *claim* outran its *implementation*, passing review because the claim was read instead of the code. Both were caught by someone re-deriving the claim. The counter is mutation, and the lesson is that a guard's docstring is a hypothesis until a mutation reds it. |
| 9 | **Noteworthy** | `vitest` strips types, so the transcript runner was 12/12 green while `tsc -p tsconfig.test.json` failed on two bare `Envelope` annotations. The test lane alone does not typecheck the test lane — `just checks` does, and it caught this. |
