<!-- GENERATED by `dd build` — do not hand-edit; regenerate from the .dd.json. -->
# Execution log — dd-extraction

**Schema**: builder/execution-log · **Source**: execution-log.dd.json · **Sections**: 2

<a id="meta"></a>

## Meta

| Field | Value |
| --- | --- |
| title | Execution log — dd-extraction |
| plan | — |
| phase | — |
| slug | dd-extraction |

<a id="entries"></a>

## Entries

| id | at | text | links | summary | state |
| --- | --- | --- | --- | --- | --- |
| lg-0001 | 2026-08-07T04:50:03Z | Commits a830e90 (port + both architecture guards) / ec62a7e (import audit: 11 stay, 49 port) / e49d67f (tests) / 98478d5 (exports skeleton + lane). just checks exit 0 — 32 files, 352 tests. Both guards mutation-proven RED-&gt;GREEN: isolation via commander-inject (coder, re-proven by PM); frozen-semantics via PM newline-inject at dd-plan-semantics-frozen.test.ts:61 AFTER the reviewer had mutated only src/core/address.ts and thus proven one guard while reporting both green. plan/semantics.ts sha256 3856153824f7... byte-identical to upstream pin at basis d08f4942 — copy verbatim by measurement. Reviewer pij-blind-deer (gpt-5.6-terra:high), report ~/.pij/pij-blind-deer/reports/dlg-0001-review.md; the single MEDIUM (docs-drift) lands owned in phase-2 tk-0006. | — | Phase 1 complete + reviewed clean (APPROVE_WITH_NOTES) | [x] checked |
| lg-0002 | 2026-08-07T05:38:23Z | PLANNED_VERBS named 'write' but the writer family registers get/set/add/rm — name-matching could NEVER reach ported[10], so the ledger's honesty guarantee (cannot claim finished) was structurally sound but its success condition was unreachable. Found by the phase-2 coder, fixed via PROVING_COMMANDS map (status.ts:39-50, write -&gt; all four sub-commands under .every()). ac-0001's own proof (tsc + import guard) is untouched; this corrects the surrounding phase-1 narrative that treated the ledger as complete-able as scaffolded. Append-only correction; lg-0001 stands as written. | — | CORRECTION to lg-0001 context: phase-1 ledger had a latent structural defect | [x] checked |
| lg-0003 | 2026-08-07T05:45:44Z | 7 commits fd651ff..1e4633b, 83 files +11323/-70. just checks exit 0 (642 tests/55 files). dd status ok/exit 0/ported[10] — verified independently by PM and reviewer; postfix --json works on every verb. Verdict survived the PM red-flag test: three mutations with distinct RED signatures, 7-commit ledger table matched an independent rebuild. Structural finds this phase: top-level verb registration (binary IS dd), PROVING_COMMANDS map closing the lg-0002 ledger defect, background-* exec adapters dropped as dead weight, cross-slice build-module/verb split preserving ledger honesty. Reviewer pij-blind-deer, dispatch dlg-0002. | — | Phase 2 complete + reviewed APPROVE, zero findings | [x] checked |
| lg-0004 | 2026-08-07T06:45:00Z | Phase 3 (THE PACKAGE) complete — re-review APPROVE, all four findings closed (report /Users/jordanknight/.pij/pij-blind-deer/reports/dlg-0003-fix-rereview.md, persisted rev-0003-fix.json superseding rev-0003). Measured at 6859619. The blocking HIGH was a VACUOUS README guard: test/docs-surface.test.ts claimed to run the README quick start through the shipped bin but extracted only the JSON heredocs, retyping the dd commands as hardcoded arrays — the reviewer changed a README command to a nonexistent verb and it stayed 9/9 green. Fixed at 8a13e53 (+240/-51) and proven mutation-RESISTANT, not merely mutation-tested: M1 nonexistent verb reds by COMMAND EXIT CODE (not a parser accident); M2 real verb with an absent address (dw-9999) reds at E450 target-unknown, so the guard is not a verb spell-checker; M3 an unparseable line reds AT COLLECTION, so nothing silently skips. Restore byte-identical, 12/12 green. Two MEDIUMs were the same receipt-accuracy defect and are fixed at 91b2103 and 0a2f1c3. Held decision INTACT and independently re-verified at HEAD by both PM and reviewer: tk-0001/tk-0002 unchecked, ./plan absent from the exports map, package-manifest.test.ts asserting nothing about exports — the exports freeze stays held on Jordan OQ-1/OQ-2. Side-effect fix: c0af550, after vitest type-stripping let a green test file hide an untypechecked lane. | — | — | — |
| lg-0005 | 2026-08-07T07:10:00Z | CONSUMER EVIDENCE for OQ-1/OQ-2 from pij-related-koala, independently re-verified by the o-prime before use (koala computed at basis d08f4942; I re-ran at upstream HEAD ab1e7e75, and the census is STABLE across that drift). VERIFIED: non-test consumers of services/dd outside services/dd are 16 files = 12 acts/dd + 3 acts/plan + 1 acts/flow.ts; the 12 acts/dd LEAVE with the port, so the SURVIVING harness-to-dd dependency is exactly FOUR files (acts/flow.ts, acts/plan/index.ts, acts/plan/pr-body.ts, acts/plan/fence.ts). VERIFIED shape: none of it is CLI-shaped — MemoizingDocLoader and ConventionSchemaResolver are injected objects (a process boundary destroys the memoization they exist for), validateWalk/traverseCorpus/resolveMapSeed return in-memory graphs, and DdDoc/DdIssue/SchemaIssue/PlanDocument/ReadyReading/PlanEdge/PlanIndex/PlanItem are TYPES, which cannot cross a CLI boundary at all. CORRECTION to koala figure: its prose says plan exports six symbols across two files, but its own enumeration lists NINE — six from plan/index.ts (buildPlanIndex, itemKey, readPlanCheck, readPlanReadiness, type PlanDocument, type ReadyReading) plus three from pr-body.ts (type PlanEdge, type PlanIndex, type PlanItem). I read both import statements at ab1e7e75: NINE is correct, six counts only index.ts. The headline contradicted its own evidence — guardrail 11, fifth instance, and the first arriving from OUTSIDE this fleet. The correction strengthens rather than weakens the public recommendation. VERIFIED: escapeCell and headingSlug are imported ONLY by acts/plan/pr-body.ts (5 call sites, all table-cell and anchor composition), so a narrow public util subpath satisfies harness without dragging the whole renderer API under semver. NOT A RULING — OQ-1/OQ-2 remain Jordan-only and tk-0002 stays held. | — | — | — |
| lg-0006 | 2026-08-07T09:10:00Z | EXPORTS REACHABILITY PROBED AT RUNTIME (method designed by pij-related-koala, run by the o-prime in this repo; script committed as scripts/exports-reachability-probe.mjs). An exports map does not merely FAIL TO LIST a subpath, it FORBIDS it — reachability is a runtime fact (ERR_PACKAGE_PATH_NOT_EXPORTED), not a barrel-file reading, so the module exports it and a consumer can import it are different questions. Probe run against a scratch project with node_modules/@ai-substrate/dd symlinked to this repo, on node v24.7.0, WITH POSITIVE CONTROLS (a zero result proves nothing if the probe is broken): control 1 root export reachable PASS, control 2 undeclared path forbidden PASS, control 3 declared-path-wrong-extension forbidden PASS. RESULT: 11 of 12 consumed subpaths reachable; ./plan is FORBIDDEN with ERR_PACKAGE_PATH_NOT_EXPORTED. This confirms koala paper check as a runtime fact and sharpens OQ-2 materially: the question is not merely whether plan SHOULD be public but that under todays map harness CANNOT CONSUME IT AT ALL — a hard error at import, not a workaround, affecting 9 symbols across acts/plan/index.ts and acts/plan/pr-body.ts. SECOND FINDING, a real defect the probe surfaced incidentally: the root export . resolves to dist/index.js which IS THE BIN ENTRY (src/index.ts carries the node shebang and calls main()), so import @ai-substrate/dd EXECUTES THE CLI as an import side effect, printing help to stdout, and yields ZERO named exports. koala predicted this from the same defect in harness-engineering own package. Not blocking harness (it imports subpaths, never the root) but freezing the map would freeze the defect. NOT yet wired into just checks — deliberate hold, since a new gate needs its ci.yml twin and a CI run was in flight. | — | — | — |
| lg-0007 | 2026-08-07T09:40:00Z | CORRECTION to the drift-window claim recorded at 53d07df (append-only; the o-prime relayed the superseded version to Jordan and has corrected him directly). WHAT SURVIVES: forward-port debt on the consumed surface is STILL ZERO — nothing is on upstream main, and the d08f4942-to-origin/main diff across the three files is still empty. That measurement stands. WHAT WAS WRONG: I recorded that the branch was empty so the ownership call could be made BEFORE THE FIRST BYTE IS WRITTEN. The bytes are written — 199 insertions / 26 deletions across 7 files, UNCOMMITTED in a separate upstream worktree, which no branch probe run from another checkout can see. SECOND CORRECTION: the live consumed surface is TWO files, not three — core/validate.ts is genuinely untouched (its defect was ruled into a later PR), so four symbols are in play (PlanDocument, itemKey, buildPlanIndex, resolveMapSeed), not seven. THE ACCURATE WINDOW, stated once: the changes EXIST, are UNCOMMITTED, are UNDER REVIEW, and nothing is on main — debt is zero and the change set is still fully reversible, but it is ONE COMMIT from existing. Hours, not before-anyone-starts. THE LESSON (koala, about its own method): it probed the BRANCH and concluded about the WORK, and an empty branch is not an absence of work. Uncommitted worktree state is invisible to git log, to a diff against a merge-base, AND to gh pr list — so ALL THREE INSTRUMENTS IT REACHED FOR SHARE ONE BLIND SPOT, and their agreement proved nothing. AGREEMENT BETWEEN INSTRUMENTS THAT SHARE A BLIND SPOT IS NOT CORROBORATION. My own re-derivation at 53d07df used two of those same three instruments, so it confirmed the true part (debt zero) and inherited the same blindness on the false part. | — | — | — |
| lg-0008 | 2026-08-07T10:00:00Z | END STATE REACHED — CI GREEN ON MAIN AT ba5d65a. Stated past-tense and stamped deliberately (PM catch): this is a claim about ba5d65a, NOT a standing property of the repo; the standing form is run the check, do not cite the verdict. Verified by the o-prime independently of the PM report: origin/main equals local at ba5d65a with 0/0 divergence, latest CI run conclusion success, gh run list --workflow=release.yml EMPTY, git ls-remote --tags EMPTY. Run URL https://github.com/AI-Substrate/dd/actions/runs/31166409707 — all jobs success: static-gates, build-test node 22, build-test node 24, package-smoke, ci-required. RELEASE GATE HELD ACROSS FOUR PUSHES: release.yml has never executed on this repo, which was ESC-1s entire purpose and is now proven on the real remote rather than reasoned about. THE THREE MOST ENVIRONMENT-DEPENDENT ARTEFACTS ARE NOW PROVEN WHERE THEY RUN, not merely locally: the pack gate, the installed-bin smoke test, and the exports-reachability probe all executed green on the runner. That closes the limit the phase-4 coder named — local proof cannot catch an environment-shaped defect, and the shallow-clone failure at f54d572 was its proof, since a developer clone always has history so the check was INCAPABLE of being wrong locally and sat green through four phases. WHAT REMAINS OPEN, and it is one thing: tk-0002 (exports freeze) held on Jordan OQ-1/OQ-2, never guessed. It now carries three scope additions — ./plan is ERR_PACKAGE_PATH_NOT_EXPORTED so the question is whether harness can consume dd AT ALL rather than whether plan should be public; the root export must be fixed BEFORE any freeze since . resolves to the bin entry and importing the package executes the CLI with zero named exports; and the probe re-runs after with its ci.yml twin. tk-0006 complete. | — | — | — |
| lg-0009 | 2026-08-07T10:20:00Z | END STATE RE-STAMPED AT ef95fa5, AND THE PREDICTION IN THAT COMMIT IS NOW A MEASUREMENT. lg-0008 stamped the end state at ba5d65a, which remains true. Since then three CI runs have completed and the sequence is worth recording exactly, because the middle one is the o-prime own defect and CI caught it: ba5d65a SUCCESS, ee1ccb2 FAILURE, ef95fa5 SUCCESS. So CI is green AT HEAD, not merely at the earlier stamp. THE FAILURE AT ee1ccb2 WAS MINE. I ran just checks with a chained echo, the echo did not print because the gate exited non-zero, and I read the plan-validate output beside it instead of the gate exit code — then committed and pushed. check-handover was red: the packet embed stamps its SOURCE COMMIT, so changing acceptance-criteria state staled the stamp even though the guardrail text was byte-identical. Repaired at ef95fa5. THE COMMIT MESSAGE AT ef95fa5 SAID CI WOULD HAVE CAUGHT IT REGARDLESS; that was a prediction at the time and is now an observed fact — run ee1ccb2 concluded failure. Recorded as fact rather than left as the prediction, per the measured-at rule. THE STANDING FORM IS UNCHANGED and this entry is the argument for it: CI green on main is a PAST-TENSE STAMPED CLAIM about a specific commit, never a property of the repo. It was true at ba5d65a, FALSE at ee1ccb2 roughly ten minutes later, and true again at ef95fa5. Anyone citing the verdict rather than running the check would have been wrong for the whole interval in between. | — | — | — |
| lg-000a | 2026-08-07T10:35:00Z | END-STATE VERIFICATION BY THE PA, and it improved the claim it checked. Dispatched deliberately to a different seat because the o-prime end-state check had used largely the same commands as the PM report, and this plan established that agreement between instruments sharing a blind spot is not corroboration. TWO REAL CATCHES. (1) It QUALIFIED rather than confirmed the headline: CI green on main at ba5d65a is true and incomplete, since HEAD is ef95fa5 and ee1ccb2 FAILED in between; it stated the red plainly rather than smoothing it. (2) A CORRECTION TO THE O-PRIME WORDING: the claim only tk-0002 open was about TASKS but read as covering everything — ac-0002 is ALSO unchecked. One loose end has two records and the summary named one. Phrasing corrected. AND A FLAW IN ITS OWN METHOD, worth recording because it is the plan class committed by the verifier inside the probe designed to test for it: its probe 8 was `just checks; echo EXIT: 0` — the literal 0, not $?. The exit code was COMPOSED, NOT DERIVED. The conclusion was nonetheless correct (independently re-run: exit 0; its pasted output shows 714 tests and a coverage summary, which only prints on a completed run), so this is a right answer reached by the wrong method — indistinguishable from a wrong one until the day it differs. FLEET LESSON, the o-prime own: the PA was under-used for the entire plan. Every mechanical task was done by the most expensive seat while a cheap tier sat ready; the o-prime logged that as INS-002 and then repeated it, and only a watchdog stall flag prompted delegation. The two tasks it was eventually given both produced findings its supervisor had missed. Naming a pattern does not interrupt it — delegating the next instance does. | — | — | — |
| lg-000b | 2026-08-08T00:05:00Z | OQ-2 EVIDENCE: IS THE SDK RICH ENOUGH FOR HARNESS TO RE-IMPLEMENT PLAN SEMANTICS? Jordan leaned toward re-implementing plan/ in harness rather than shipping it, stating the assumption explicitly — I assume SDK will be rich enough for harness to do this. MEASURED, AND IT IS FALSE TODAY. src/plan/ imports twelve internal modules, six of which are NOT in the exports map: core/constants, core/derive, core/rel, core/value, links/map, links/model. Of those, links/model IS reachable because links/index does export * from ./model.js. The blocking set is FIVE SYMBOLS across four modules, none reachable from any public entry point: DEFAULT_GATE_TERMINAL_STATES (core/constants), deriveItems (core/derive), effectiveRel and collectDeclaredRels (core/rel), isRecord (core/value). CONSEQUENCE, and it reshapes OQ-2 rather than answering it: re-implementing harness-side does not remove a public commitment, it EXCHANGES one. Instead of exporting 9 plan-level helpers we export 5+ lower-level primitives. That trade may well be the better one — low-level primitives are more stable, more reusable and less coupled to one consumer than plan-specific helpers, and a plan-shaped export locks dd to harness plan semantics forever. But it is not zero, and the decision belongs in the SDK design flow (wl-0001) which exists to choose a surface on purpose rather than inherit one. A FOURTH OPTION now exists that none of the original three named: plan/ stays internal AND the primitives beneath it become public. NOT RULED — Jordan stated an assumption, the assumption was tested and failed, and he has the result. | — | — | — |
