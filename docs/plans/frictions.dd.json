{
  "dd": {
    "schema": "project/friction"
  },
  "sections": [
    {
      "name": "meta",
      "value": {
        "title": "dd — friction ledger",
        "owner": "pij-mental-dajeil (o-prime, dd)",
        "note": "Things that made dd harder to use than it should be, reported by REAL CONSUMERS driving it in repos that are not this one. Opened 2026-08-08 on Jordan's ask, as a sibling to the wishlist — deliberately separate, because a wishlist row is work someone wants and a friction row is EVIDENCE someone hit. Only some evidence becomes work, and the ledger is worth more if that conversion is a decision rather than an assumption. Jordan routes dd frictions here from other fleets. A row stays open until it is fixed, ruled wontfix, or fails to reproduce — never because it got old."
      }
    },
    {
      "name": "frictions",
      "value": [
        {
          "id": "fr-0001",
          "title": "builder schema packages exist ONLY in harness-engineering, so dd-native /builder works nowhere else",
          "severity": "blocking",
          "reported_by": "pij-disturbing-ox",
          "reported_from": "chainglass, dd-builder dogfood",
          "what_happened": "First command in a consuming repo. `harness plan new <slug>` writes plan.dd.json and assets/tasks/phase-1/tasks.dd.json, then FAILS to render either sibling: 'schema \"builder/plan\" was not found in any discovery root'. All four discovery roots probed and empty — <plandir>, <repo>/.dd, <repo>/.harness/.dd, ~/.dd. The builder/{plan,tasks,backpressure,execution-log,review,fence} packages exist only in harness-engineering/.dd/schemas/. Net effect: dd-native /builder plan works in harness-engineering and in no other repo.",
          "workaround": "cp -R ~/substrate/harness-engineering/.dd/schemas/builder ./.dd/schemas/ — after which plan render and dd set both work.",
          "asked_for": "The extracted dd package should SHIP these schema packages, or seed them on first use. Otherwise every consuming repo needs a manual copy that nobody documents.",
          "state": "confirmed",
          "independently_hit_by": "pij-mental-dajeil, 2026-08-07, plan 001 — captured as DL-002 with the same workaround (hand-copied the builder family out of harness-engineering) and the same diagnosis (no `harness dd schema install`, no ship-with-CLI mechanism). Two fleets, two repos, identical wall.",
          "note": "CONFIRMED rather than merely open: hit independently by two agents in two repos before either knew of the other. That is as strong as a friction report gets. It also bears directly on plan 002 — an SDK that cannot resolve its own first-party schemas in a consuming repo is not consumable, whichever distribution method ships it. Candidate shapes, not yet ruled: ship the schemas inside the package and add them as a discovery root; or a `dd schema install <family>` verb; or seed on first use. The first keeps them versioned with the code that reads them, which argues for it."
        },
        {
          "id": "fr-0002",
          "title": "16 of builder/plan's 22 declared sections can never be created — the section set is frozen at scaffold time",
          "severity": "hard-wall",
          "reported_by": "pij-disturbing-ox",
          "reported_from": "chainglass, dd-builder dogfood",
          "what_happened": "builder/plan declares 22 sections; `harness plan new` scaffolds only 6 (meta, summary, goals, non_goals, acceptance_criteria, phases). The other 16 — testing_strategy, documentation_strategy, key_findings, gate_matrix, risks, risks_assumptions, open_questions, clarifications, planning_seam, acceptance_coverage, implementation_summary, execution_guardrails, research_context, workshop_opportunities, tasks, done_when — CANNOT BE CREATED. `dd set` and `dd add` both refuse with E450 section-unknown ('the document has no section X'), and there is no add-section verb: dd has get/set/add/rm, and `rm` can DELETE a section but nothing creates one. So the section set is frozen at scaffold time, permanently.",
          "workaround": "Hand-edit plan.dd.json then `harness dd build` — which the builder skill explicitly forbids ('never hand-edit plan.dd.json'), even though the dd README sanctions it. So the only escape route is one the calling skill prohibits.",
          "asked_for": "Either `plan new` scaffolds every declared section empty, or set/add auto-create a schema-DECLARED section. E450 should fire only for sections the SCHEMA does not declare — not for ones the document merely lacks.",
          "state": "confirmed",
          "independently_hit_by": "pij-mental-dajeil, 2026-08-07, plan 001 — captured as DL-003, same 6-of-22 count, same E450, same conclusion that no verb can add a section. Workaround used there was a structural JSON insert of empty sections followed immediately by plan validate + render to prove schema-validity and no drift.",
          "note": "The sharpest part of ox's report is the one I did not make: this DIRECTLY BREAKS /builder 1b plan, whose module instructs the agent to author key_findings, gate_matrix, clarifications, planning_seam, testing_strategy, risks and open_questions — none of which dd can hold. So a skill and a tool that ship together disagree about what is possible, and the only bridge between them is an action one of them forbids. E450's message is also arguably the real defect: 'the document has no section X' is TRUE and useless, because it describes the document when the actionable fact is whether the SCHEMA allows X. An error that names the wrong authority sends the reader to the wrong fix."
        }
      ]
    }
  ]
}
