<!-- GENERATED by `ddocs build` — do not hand-edit; regenerate from the .dd.json. -->
# dd — friction ledger

**Schema**: project/friction · **Source**: frictions.dd.json · **Sections**: 2

<a id="meta"></a>

## Meta

| Field | Value |
| --- | --- |
| title | dd — friction ledger |
| owner | pij-mental-dajeil (o-prime, dd) |
| note | Things that made dd harder to use than it should be, reported by REAL CONSUMERS driving it in repos that are not this one. Opened 2026-08-08 on Jordan's ask, as a sibling to the wishlist — deliberately separate, because a wishlist row is work someone wants and a friction row is EVIDENCE someone hit. Only some evidence becomes work, and the ledger is worth more if that conversion is a decision rather than an assumption. Jordan routes dd frictions here from other fleets. A row stays open until it is fixed, ruled wontfix, or fails to reproduce — never because it got old. |

<a id="frictions"></a>

## Frictions

| id | title | severity | reported_by | reported_from | what_happened | workaround | asked_for | state | independently_hit_by | note |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| fr-0001 | builder schema packages exist ONLY in harness-engineering, and the CLI's own remediation for the miss is a dead end | blocking | pij-disturbing-ox | chainglass, dd-builder dogfood | First command in a consuming repo. `harness plan new &lt;slug&gt;` writes plan.dd.json and assets/tasks/phase-1/tasks.dd.json, then FAILS to render either sibling: 'schema "builder/plan" was not found in any discovery root'. All four discovery roots probed and empty — &lt;plandir&gt;, &lt;repo&gt;/.dd, &lt;repo&gt;/.harness/.dd, ~/.dd. The builder schema packages exist only in harness-engineering/.dd/schemas/, and there are FIVE of them: plan, backpressure, execution-log, fence, review. Net effect: dd-native /builder plan works in harness-engineering and in no other repo. | cp -R ~/substrate/harness-engineering/.dd/schemas/builder ./.dd/schemas/ — after which plan render and dd set both work. | Seed the schema packages on `plan new` — pij-massive-meadowlark's recommendation, and it stays inside the documented four-root precedence rather than adding a fifth bundled root. (Shipping them inside the package as a new root is the alternative; it keeps schemas versioned with the code that reads them, but changes resolution order for every consumer.)<br><br>**TRACKED UPSTREAM**: AI-Substrate/harness-engineering#119 — ALREADY OPEN there from the same chainglass source before we relayed it; we did not know, and two ledgers were carrying one item. Accepted by pij-massive-meadowlark, which owns the flow and the harness. Severities preserved as filed; ox's corollary quoted on its rows. (This schema has no tracking field, so the issue ref lives in prose — noted as a gap rather than papered over.) | planned | pij-mental-dajeil, 2026-08-07, plan 001 — captured as DL-002 with the same workaround (hand-copied the builder family out of harness-engineering) and the same diagnosis (no `harness dd schema install`, no ship-with-CLI mechanism). Two fleets, two repos, identical wall. | CONFIRMED rather than merely open: hit independently by two agents in two repos before either knew of the other. That is as strong as a friction report gets. It also bears directly on plan 002 — an SDK that cannot resolve its own first-party schemas in a consuming repo is not consumable, whichever distribution method ships it. Candidate shapes, not yet ruled: ship the schemas inside the package and add them as a discovery root; or a `dd schema install &lt;family&gt;` verb; or seed on first use. The first keeps them versioned with the code that reads them, which argues for it. CORRECTION 2026-08-08, and the correcting reporter is the one who caught it: ox first said SIX packages including builder/tasks. There is no builder/tasks — assets/tasks/phase-N/tasks.dd.json declares schema 'builder/plan' and uses its `tasks` and `done_when` sections. Re-derived independently by pij-massive-meadowlark and again here (`ls .dd/schemas/builder` → five; the task file's own `dd.schema` field → builder/plan). I had TRANSCRIBED ox's six into this row without deriving it, which is the plan-001 class arriving in a friction ledger. |
| fr-0002 | 16 of builder/plan's 22 declared sections can never be created — the section set is frozen at scaffold time | hard-wall | pij-disturbing-ox | chainglass, dd-builder dogfood | builder/plan declares 22 sections; `harness plan new` scaffolds only 6 (meta, summary, goals, non_goals, acceptance_criteria, phases). The other 16 — testing_strategy, documentation_strategy, key_findings, gate_matrix, risks, risks_assumptions, open_questions, clarifications, planning_seam, acceptance_coverage, implementation_summary, execution_guardrails, research_context, workshop_opportunities, tasks, done_when — CANNOT BE CREATED. `dd set` and `dd add` both refuse with E450 section-unknown ('the document has no section X'), and there is no add-section verb: dd has get/set/add/rm, and `rm` can DELETE a section but nothing creates one. So the section set is frozen at scaffold time, permanently. | Hand-edit plan.dd.json then `harness dd build` — which the builder skill explicitly forbids ('never hand-edit plan.dd.json'), even though the dd README sanctions it. So the only escape route is one the calling skill prohibits. | Either `plan new` scaffolds every declared section empty, or set/add auto-create a schema-DECLARED section. E450 should fire only for sections the SCHEMA does not declare — not for ones the document merely lacks.<br><br>**TRACKED UPSTREAM**: AI-Substrate/harness-engineering#136 (meadowlark's half). Accepted by pij-massive-meadowlark, which owns the flow and the harness. Severities preserved as filed; ox's corollary quoted on its rows. (This schema has no tracking field, so the issue ref lives in prose — noted as a gap rather than papered over.) | planned | pij-mental-dajeil, 2026-08-07, plan 001 — captured as DL-003, same 6-of-22 count, same E450, same conclusion that no verb can add a section. Workaround used there was a structural JSON insert of empty sections followed immediately by plan validate + render to prove schema-validity and no drift. | The sharpest part of ox's report is the one I did not make: this DIRECTLY BREAKS /builder 1b plan, whose module instructs the agent to author key_findings, gate_matrix, clarifications, planning_seam, testing_strategy, risks and open_questions — none of which dd can hold. So a skill and a tool that ship together disagree about what is possible, and the only bridge between them is an action one of them forbids. E450's message is also arguably the real defect: 'the document has no section X' is TRUE and useless, because it describes the document when the actionable fact is whether the SCHEMA allows X. An error that names the wrong authority sends the reader to the wrong fix. |
| fr-0003 | `dd schema list` reports ok with zero schemas — the repo's own never-fake-success rule, violated by the repo | degrading | pij-disturbing-ox (under-sold in its own report); proven here by pij-mental-dajeil | chainglass dogfood → re-derived in a clean scratch repo | When `builder/plan` fails to resolve, acts/plan/index.ts:298 tells the reader to run `harness dd schema list` to see which roots are searched. In a repo with no schemas that command returns `status: ok`, `schemas: []`, three roots — and `next_action: "Run `dd schema show &lt;name&gt;` for one schema in full."`, which points at the set it has just proven empty. So the CLI's own remediation path is a DEAD END BY CONSTRUCTION: it reports success on a state that is definitionally unconfigured, and hands the reader an instruction that cannot be followed. | None needed for the reader who already knows the answer; there is none for the reader who does not, which is the entire point. | Zero resolving schemas is `unconfigured` (exit 2), not `ok` — and the next_action should name how to GET schemas, not how to inspect the none that exist. The roots list is the right evidence to print; the verdict and the next step are the defects. | confirmed | Proven directly rather than reported: `git init` in an empty dir, then `dd schema list --json` from this repo's bin. Re-derive with exactly that. | THIS IS OUR OWN HEADLINE RULE BROKEN BY OUR OWN CLI. AGENTS.md and orient-local both state it: 'unconfigured means nothing is mapped here yet, never it worked — the repo's green-but-wrong defence, enforced in code, not prose.' Zero schemas resolving IS nothing-mapped-here-yet, and the verb returns ok. Same family as a probe that cannot detect and a gate that cannot fail: an instrument that answers confidently about a question it did not actually examine. Found by following ox's lead on a point it explicitly said it had under-sold — worth noting, because the sharpest finding in a report is not always the one the reporter leads with. |
| fr-0004 | The backpressure seam is not dd-aware — the survey and the plan cannot link to each other | hard-wall | pij-disturbing-ox | chainglass, dd-builder dogfood | eng-harness-flow's backpressure verb module declares Consumes: docs/plans/&lt;ord&gt;-&lt;slug&gt;/&lt;slug&gt;-plan.md and Produces: assets/backpressure-coverage.md. In a dd-native plan folder that consumed file DOES NOT EXIST — the plan is plan.dd.json plus a generated plan.dd.md — and the module's PHASE 0 step 1 says 'If neither exists, there is nothing to survey against; say so and STOP.' So an agent following the module literally STOPS on every dd-native plan. Worse, the artifact it is told to produce is MARKDOWN, while builder/plan's `pressure` fields target builder/backpressure/section/rows — so a markdown survey can never be linked from an acceptance criterion or a done_when assertion. | Authored BOTH: assets/backpressure.dd.json (authoritative, 11 rows, pressure-linked from all 8 ACs and all 16 done_when assertions) plus the markdown the module contractually requires. Two artifacts, one truth, maintained by hand. | The verb should consume plan.dd.json and emit backpressure.dd.json, with the markdown as its GENERATED sibling.<br><br>**TRACKED UPSTREAM**: AI-Substrate/harness-engineering#136. Accepted by pij-massive-meadowlark, which owns the flow and the harness. Severities preserved as filed; ox's corollary quoted on its rows. (This schema has no tracking field, so the issue ref lives in prose — noted as a gap rather than papered over.) | planned | Not yet — this repo's plan 001 never ran the backpressure seam against a dd-native plan, so the wall was never reached here. | ox's own closing line is the finding: two artifacts, one truth, maintained by hand — EXACTLY THE DRIFT dd EXISTS TO KILL. The seam that is supposed to establish how done-ness will be proved is itself unproved against the document format the same toolchain produces. Note the shape it shares with fr-0002: a skill and a tool that ship together disagree about what exists, and the agent is left to bridge them by hand. That is now twice, which makes it a pattern rather than an incident — the dd-native and markdown-native halves of the builder toolchain were specified against different realities. |
| fr-0005 | builder/backpressure schema and the backpressure module disagree on vocabulary, silently | degrading | pij-disturbing-ox | chainglass, dd-builder dogfood — filed because the o-prime asked for the one nearly not filed | builder/backpressure/schema.json declares certainty enum [Partial, Confident, Proven] and proof_tier enum [computational, human-judgement]. The backpressure verb module specifies certainty Strong\|Partial\|Weak (STEP 4 — three named rungs with explicit definitions) and three tiers computational\|inferential\|human-judgement. So 'Strong' and 'Weak' — two of the module's three verdicts — are UNREPRESENTABLE in the dd document, and 'inferential', which the module names as its own middle tier and uses throughout, does not exist in the schema. | Landed on Partial, which validated only BY COINCIDENCE — it is the one value both vocabularies happen to share. | Reconcile the two, and pick a direction deliberately. The module's three-rung certainty is load-bearing prose (it drives the closing verdict's four-rung ladder); the schema's Confident/Proven have no definition anywhere ox could find.<br><br>**TRACKED UPSTREAM**: AI-Substrate/harness-engineering#136. Accepted by pij-massive-meadowlark, which owns the flow and the harness. Severities preserved as filed; ox's corollary quoted on its rows. (This schema has no tracking field, so the issue ref lives in prose — noted as a gap rather than papered over.) | planned | Not yet. | THE FAILURE MODE IS THE TIMING, and it is why this outranks its severity label: a survey reaching a STRONG verdict would have been refused by the schema AFTER the analysis was already done, with no way to record the conclusion it had reached. The tool does not prevent the wrong answer — it prevents recording the right one, and only at the end. That is worse than a validation error at the start, because the cost is paid in work already completed. Two vocabularies for one concept, in a toolchain whose entire premise is single-definition — the same two-vocabularies hazard this repo used to argue plan/ should not be split across two codebases. ox nearly did not file this one; it is the sharpest of the four. |
| fr-0006 | dd-rendered plans break every downstream consumer that scrapes per-phase headings — SILENTLY | blocking | pij-disturbing-ox | chainglass, dd-builder dogfood | The legacy markdown plan contract emits '#### Phase Index' plus a '#### Phase N: &lt;Title&gt;' block per phase, and downstream tooling extracts by that heading. builder/plan's renderer instead emits ONE '## Phases' section containing a TABLE — no per-phase heading exists at any level. flow-pair's ContextPackCompiler.extractSection matches a heading by exact / prefix-colon / prefix-space and captures to the next same-or-higher heading; with --phase 'Phase 1: Implementation' it returns {ok:false, section not found} against a dd plan. So a delegation packet COMPILES WITH THE PLAN SECTION MISSING, and the miss is recorded only as an exclusion. The same assumption sits in builder's own 20-plan and 50-phase-tasks modules, which describe per-phase blocks as the output contract. | --phase 'Phases' (matches the table section) plus --tasks-dir, since tasks.dd.md DOES render per-task '### tk-NNNN' headings under '## Done when'. | Either the plan renderer emits a per-phase heading block alongside the index table, or the consumers move to ADDRESSING ROWS (plan.dd.json#phases/ph-0001) instead of scraping headings. ox's own read, and it is right: the second is the whole point of dd.<br><br>**TRACKED UPSTREAM**: AI-Substrate/harness-engineering#136. Accepted by pij-massive-meadowlark, which owns the flow and the harness. Severities preserved as filed; ox's corollary quoted on its rows. (This schema has no tracking field, so the issue ref lives in prose — noted as a gap rather than papered over.) | planned | Adjacent hit here, plan 001, DL-005 (2026-08-07): flow-pair's context-pack compiler reported '(no tasks found)' for a dd-format plan because it globs tasks.md while dd repos render tasks.dd.md — same compiler, same class, different file. So the compiler has now missed dd content twice by scraping for markdown shapes dd does not emit. | THE SEVERITY IS THE SILENCE, NOT THE MISS. It does not error loudly — the worker simply gets less context than the orchestrator believes it sent, and the only trace is an exclusion line nobody reads. An orchestrator cannot tell a well-briefed worker from a starved one, so a bad outcome downstream looks like a bad worker. That is the reports-success-while-broken family, and it is the third instance in this ledger (fr-0003 returns ok on zero schemas; fr-0005 refuses the right answer only at the end). ox's closing line is the sharpest thing in the whole ledger and it names what plan 002 is for: THE GRAPH EXISTS WHILE THE CONSUMER STILL SCRAPES. dd's entire premise is that structure is addressable — plan.dd.json#phases/ph-0001 is exact, stable and typed, while a heading match is a guess about layout. Every consumer still scraping is a consumer that has not been given a reason to stop, and giving them that reason IS the SDK. This row is therefore a plan-002 design input, not merely a bug. |
| fr-0007 | `harness plan new` has no ordinal, and the folder convention does — a slug that must secretly contain one | degrading | pij-disturbing-ox | chainglass, dd-builder dogfood — 'nearly binned' | builder's contract is PLAN_DIR = docs/plans/&lt;ordinal&gt;-&lt;slug&gt;/ and its own module states it twice. But the scaffolder signature is `harness plan new &lt;slug&gt; --dir docs/plans` — no --ordinal, and no awareness the ordinal exists. Passing the FULL '092-terminal-prompt-drawer' as the slug works, but only by guessing. Passing the natural bare slug 'terminal-prompt-drawer' — which is what the flag is named for and what every doc calls a slug — SILENTLY creates a second folder docs/plans/terminal-prompt-drawer/, leaving the flight plan pointing at a folder with no plan and the plan in a folder with no flight plan. No error either way; both are valid directories. It then writes meta.slug from that same string, so the document records the folder name as its slug. | Pass the full '&lt;ordinal&gt;-&lt;slug&gt;' as the slug argument, which only occurs to someone who already knows. | Either `plan new` takes --ordinal and composes the folder, or it reads the next ordinal the way the-flow does, or its argument is RENAMED to what it actually is (the folder name).<br><br>**TRACKED UPSTREAM**: AI-Substrate/harness-engineering#136. Accepted by pij-massive-meadowlark, which owns the flow and the harness. Severities preserved as filed; ox's corollary quoted on its rows. (This schema has no tracking field, so the issue ref lives in prose — noted as a gap rather than papered over.) | planned | Adjacent hit here, plan 001 (DL-001): `harness plan new` cannot target an EXISTING plan folder — no --folder flag — so it minted docs/plans/dd-extraction beside the real docs/plans/001-dd-extraction created at explore time, fixed by rsync+rm. Same verb, same root cause: it does not know the folder convention its own contract declares. | ox's closing line is the finding: A SLUG THAT MUST SECRETLY CONTAIN AN ORDINAL IS A TRAP THAT ONLY FIRES FOR PEOPLE WHO DID THE ORDINAL STEP CORRECTLY FIRST. The naming is the defect, not the missing flag — an argument called `slug` that must not be a slug will mislead every reader who trusts it, and reward only those who ignore it. Note the shape shared with DL-001 above: both are the scaffolder being unaware of a convention its own documentation states, and both fail by SILENTLY CREATING A SECOND FOLDER rather than refusing. Two folders is a worse outcome than an error, because nothing is broken enough to notice. |
| fr-0008 | Validation gets LOUDER the more correctly you build the graph — 34 warnings for doing it right | degrading | pij-disturbing-ox | chainglass, dd-builder dogfood — 'nearly binned', and the sharper of the pair | `harness dd validate` on a freshly-scaffolded plan returned 34 WARN, every one E432 address-target-untracked ('address target is not tracked by git — track it with git add'). A plan scaffolded seconds earlier is untracked BY DEFINITION, so `plan new` followed by `validate` ALWAYS produces this — and the count scales with LINK COUNT. ox hit 34 purely because it had wired satisfies + pressure links on every task and assertion. `git add` dropped it to 0 error / 0 warn. | git add the plan folder before validating. | Suppress untracked-target warnings for paths INSIDE the document's own plan folder, or fold them into ONE summary line naming the count and the single `git add` that clears them — rather than one row per link. | open | Not yet — plan 001 authored its links after the folder was tracked, so the ordering hid it. | THIS INVERTS THE REPORTS-SUCCESS-WHILE-BROKEN FAMILY AND ARRIVES AT THE SAME PLACE: not reporting success while broken, but REPORTING ALARM WHILE CORRECT. **Doing the dd thing properly is what made the report noisy** — a lazy plan with no links would have validated quietly. An instrument that punishes correct use trains its user to ignore it, which is the same terminus as an expected-red note (orient-local § where mechanism cannot reach, item 2) reached from the opposite direction: there a supervisor tells you to ignore a gate; here the gate teaches you by itself. ox notes plan-ready.md already rules on this in its own words — that per-row warnings on a mid-flight plan teach a reader to ignore warnings — so the doctrine exists and the verb does not honour it. **A warning whose volume scales with how well you used the tool is a design defect, not a tuning problem.** |
| fr-0009 | Authoring a dd plan breaks the consuming repo's lint gate — and dd has no stated formatting contract | degrading | pij-disturbing-ox | chainglass, dd-builder dogfood — captured there as DL-004 | After authoring plan.dd.json + three tasks.dd.json and copying .dd/schemas, `just lint` (biome) went from green to 13 errors. Every one is format-only, and every one is on a GENERATED or ledger artifact — the dd documents, the schema packages, and the flow-pair and pij ledgers. ZERO from product code. It is not fixable by formatting: `dd build` / `dd set` rewrite the .dd.json their own way on the next mutation, so a hand-formatted file is re-broken by the next legitimate dd write — and `dd build --check` would then fail if someone formatted to satisfy biome. **Two generators, one file, mutually exclusive outputs**, so a repo whose lint gate covers JSON oscillates between red lint and dd drift. | Added docs/plans/**/*.dd.json, .dd/schemas/**, .flow-pair/**, .pij/** to biome.json ignores. Green across 1772 files. | dd should STATE its formatting contract: either (a) emit formatter-compatible output so a consumer's formatter is satisfied by the generator, or (b) document the required ignore globs as part of adoption, next to the schema seeding from fr-0001. ox's read, and it is right: (b) is cheap and honest; (a) is better but couples dd to whatever formatter the consumer runs. Either way an adopting repo currently discovers this by its CI going red. | open | NOT hit in AI-Substrate/dd — and the reason matters. Checked rather than assumed: this repo's biome.json uses an ALLOWLIST (`src/**/*.ts`, `test/**/*.ts`, `bin/**/*.js`, `*.ts`), so JSON is never in lint scope at all. **We are immune BY ACCIDENT, not by design** — nobody decided generator-owned artifacts should be out of scope; we simply never put JSON in. So dd ships with no formatting contract and a home repo that cannot discover the gap. | THE SHARP PART IS OX'S, AND IT IS A NEW SHAPE FOR THIS LEDGER: chainglass's biome.json ALREADY ignored `docs/plans/**/the-flow*.json`. So this exact class had been hit before and patched NARROWLY — for the-flow's JSON specifically. dd's artifacts arrived later, into the same folder, under the same generator discipline, and were not covered. **The lesson was learned about one file pattern instead of about the category 'generator-owned artifacts are not hand-formattable'.** ox's prediction is the finding: the next generator to land in docs/plans/ hits it a third time. This is the inverse of our own P-7 (`the urge to consolidate is the urge to overstate coverage`) and shows the other cliff: **P-7 warns against merging rules whose ACTIONS differ; this warns against patching an INSTANCE when the CAUSE is a category.** Consolidate causes, never instruments — and a glob is an instrument. A fix that names one path teaches nothing; a fix that names the category retires the class. |
| fr-0010 | FsDocLoader is load-bearing, lives in the half that LEAVES, and is not exported — the injection pattern is broken at its joint | blocking | pij-related-koala (harness-side prime, the actual consumer) | harness-engineering ↔ dd, read-only, before any trial | harness/cli/src/acts/flow.ts:126 constructs the injected pair as `new MemoizingDocLoader(new FsDocLoader(deps.fs, new NodeHash(), null))`. MemoizingDocLoader IS reachable — it is in ./links. **FsDocLoader is NOT.** It lives at harness acts/dd/shared.ts:119, one of the TWELVE files that LEAVE with the port, and at dd src/acts/shared.ts:108 — our CLI half. The exports map has no ./acts and no ./shared. So on the day harness deletes acts/dd it loses FsDocLoader, and the SDK offers no replacement: **harness can construct the memoizer but not the loader it memoizes.** The injection pattern — the very thing that made library consumption forced rather than preferred (OQ-1) — is broken at exactly its load-bearing joint. | None available to the consumer. It cannot import what is not exported, and deep-pathing into ./acts is the thing the exports map exists to prevent. | koala's recommendation, and mine after verifying: **(b) MOVE FsDocLoader into the SDK half.** It is small and its dependencies are ALREADY PUBLIC — Pick&lt;FsPort,'readText'&gt;, HashPort, a ReadonlySet, and parse(). Nothing about it is CLI except where it currently sits. Rejected: (a) export ./acts/shared — that publishes CLI internals and freezes OQ-2 around them. Rejected: (c) harness re-implements against the exported DocLoader interface — possible, but it creates a SECOND answer to 'what is a readable dd document', and two would drift. koala explicitly does not want (c), which is notable because (c) is the option that costs it work and costs us none. | confirmed | Verified here before recording, not accepted: FsDocLoader at src/acts/shared.ts:108; package.json exports lists no ./acts and no ./shared; the DocLoader interface IS at src/core/walk.ts:17 and ./core/walk IS exported; the constructor takes Pick&lt;FsPort,'readText'&gt;, HashPort, ReadonlySet&lt;string&gt;\|null — all small, none CLI-bound. Every one of koala's claims holds. | **THIS IS WHY THE TRIAL BAR WAS SENT TO THE CONSUMER BEFORE IT WAS DEFINED, and the asking paid immediately.** Our candidate bar — 'a fixture RE-IMPLEMENTING plan validate through public exports only' — WOULD NOT HAVE CAUGHT THIS, and koala's reason is the sharpest thing in the exchange: **a clean-sheet author never constructs FsDocLoader**, because fresh code against an SDK reaches for whatever the SDK offers and builds its own loader or a fake. The real integration is not clean-sheet — it ADAPTS four existing files whose construction shape is already fixed. A re-implementation tests *can the SDK do the job*; it does not test *can THIS CALLER do its existing job through the SDK*, **and only the second is the trial.** The corrected bar therefore reproduces THE INJECTION, not the feature: construct ConventionSchemaResolver and MemoizingDocLoader **with a fixture-owned foreign fs port**, because ownership running INWARD is the property that cannot be shelled and therefore the property the trial exists to prove. If an outside caller cannot pass its own port, library consumption fails at the joint that made it mandatory. koala also corrected its own first assumption honestly: in-repo is weaker but NOT blind, since Node self-reference traverses the exports map — the real gap is PACK INCLUSION, because a path can be correctly exported and absent from the tarball, and those are two different failures. Hence its ask that the fixture ride the pack-gate's INSTALLED TARBALL so one artifact proves both. |
| fr-0011 | `harness flow apply` writes junk keys onto a node and reports ok — the documented gate-move example is malformed, so doctrine-built spines are ungated past phase 1 | blocking | pij-disturbing-ox (observation); mechanism corrected by pij-massive-meadowlark, which owns the flow | chainglass, dd-builder dogfood | **READ THE MECHANISM HERE, NOT IN `note` — the original diagnosis in this row was REFUTED by the owner on 2026-08-09 after it reproduced rather than accepting the report.** The OBSERVATION stands and is confirmed: following the documented gate-move produces a spine whose phase gates do not exist. The CAUSE is narrower and worse than 'set is broken'. `set` works. The worked example in `skills/builder/references/flight-plan-ops.md:78-79` — the expander's gate-move, which is OUR OWN DOCUMENTATION — is malformed against the real op schema in BOTH of its ops. Op 2 nests fields under `node` while parseOp reads them top-level, so run verbatim the batch is REFUSED LOUDLY (E108, 'op #1 is missing a string id') and the gate move never happens. Run op 1 ALONE and it returns ok while writing junk keys `path` and `value` onto the node and leaving `dd_link` untouched, because `mergeInto` (harness/cli/src/services/flow/flow-mutations.ts:1341-1351) has NO FIELD ALLOWLIST and writes any key an op carries straight onto the node. **So it is not a silent no-op — it is silent schema corruption reporting success.** The `ok` was honest about a write; just not the intended one. ox did not improvise the shape. We taught it. | **REPAIR IS PROVEN. THE CAVEAT THIS ROW CARRIED IS DISCHARGED, AND IT WENT THE GOOD WAY.** An apply-written `dd_link` DOES fire the gate — measured differentially by pij-concerned-mule, re-derived at source by meadowlark before it was sent. Two IDENTICAL harness-loop flows from one template, `dd_link` wired into ONE through apply only, against a real tasks file with one item checked and one unchecked. **(A) GATED** → `nav set` exits 1 with **E440** naming the incomplete item, `nav.now` STAYED PUT, nothing written. **(B) CONTROL, no dd_link** → same command, exit 0, cursor moved. **(C) tick the last item, re-run A** → exit 0, cursor moves, and the node records `{"status":"complete","terminal":2,"total":2}`. **THE LEGS DISAGREE — that is the control the earlier probe lacked, and the reason the claim is now assertable when it was not before.** A probe whose control passes too has measured nothing. Repair shape: one `apply` per phase node with `{address, gate:true}`. **AND THE ACCEPTANCE TEST IS NOW RUN, BY THE ONE SEAT PLACED TO RUN IT.** ox had a phase with SIX genuinely incomplete task rows — a real incomplete target, which neither of the other probes had. It repaired `phase-2` through apply, then attempted the departure: `harness flow nav set --now review-2` → **status error**, *'node "phase-2" gates on "…/assets/tasks/phase-2/tasks.dd.json#tasks": 6 of 6 items are not complete (tk-0101 (unchecked), tk-0102 …, tk-0106 (unchecked))'*, and nav stayed on phase-2. **THE REFUSAL ENUMERATED THE FILE'S REAL ROW IDS AND STATES — a gate that was well-formed but inert could not have produced that list.** So an apply-written dd_link is not merely persisted; it is loaded and enforced on departure identically to a template-baked one. Carry this to #135 as the acceptance test, not as a second opinion. | Owned by meadowlark as AI-Substrate/harness-engineering#135, accepted 2026-08-09. (1) Give `mergeInto` a field allowlist so an op carrying an unknown key is REFUSED, not merged. (2) Fix the malformed worked example at flight-plan-ops.md:78-79 — it is the origin. (3) Expose `--dd-link` on set-node/insert-node so repair does not require hand-written JSON. (4) Make `sanitizeDdLink` refuse an unrecognised key instead of discarding it silently.<br><br>**TRACKED UPSTREAM**: AI-Substrate/harness-engineering#135 — coder active, item-5 result posted. Accepted by pij-massive-meadowlark, which owns the flow and the harness. Severities preserved as filed; ox's corollary quoted on its rows. (This schema has no tracking field, so the issue ref lives in prose — noted as a gap rather than papered over.) | planned | **THE STATE LABEL COVERS THE OBSERVATION ONLY.** The enum has no value for 'observation confirmed, diagnosis refuted', which is this ledger's own instance of the governance line 'a label must name which claim it covers' — so the claim it covers is named here in prose instead. Confirmed: following the doctrine yields an ungated spine. **REFUTED AT THE MECHANISM, not merely at the observation**: `ddLinkGates` (flow-events.ts:197-199) is `link.gate !== false` — a bare `{address}` gates, `gate:true` gates, and ONLY `gate:false` opts out; `evaluateDdGate` reads the field live off the node on every departure, and there is NO `create --template` provenance anywhere in that path. So 'only `create --template` can write a working dd_link' is not just unobserved, it is impossible — **a spine repaired through apply is a genuinely gated spine.** OUR CROSS-REPO EVIDENCE IS UNAFFECTED AND MEADOWLARK SAYS IT IS WHAT MADE THE CAUSE FINDABLE. Plan 001's four phases ALL carry dd_link and phase-3's demonstrably refused a premature departure on two unchecked tasks; review-1/2/3 carry NONE while review-4 does. The template-baked-versus-later-added split is explained BETTER by the true cause than by the original: the documented gate-move either errors out entirely or writes junk, so every spine built by following the doctrine is ungated past phase 1 — exactly where both splits fall. meadowlark: 'I would not have looked at the doctrine file without it.' | **WHY THE EARLIER PROBE WAS BLIND — measured, not guessed, and the most reusable thing here.** `departureGate` (flow-mutations.ts:135-197) has exactly ONE call site: `setNow`, line 298. `flow status --to done` never reaches it, so it returns 'allowed' for gated and ungated nodes alike. **The first probe could not have answered either way, in either direction** — it was not a weak signal, it was no signal, and it read as reassurance. Departure gates fire on `nav set`, never on status. **A THIRD SILENT DROP, WHICH NOBODY HAD — AND ITS FIRST DIAGNOSIS WAS ALSO WRONG, CORRECTED BY ITS OWN AUTHOR.** `sanitizeDdLink` REFUSES an invalid check loudly (E108) but SILENTLY DISCARDS an unrecognised key, so `{"kind":"plan-complete","address":…}` is written as `{"address":…}`. This row previously said that yields *'ok, and no gate'*. **REFUTED in both address shapes, measured by meadowlark's coder: NOTHING IS LEFT UNGATED. The dropped key does not OMIT a gate — it SILENTLY SUBSTITUTES A DIFFERENT ONE, and which one depends on the address:**<br><br>- **SECTION address** (`docs/tasks.dd.json#tasks`): the semantic `plan-validate` check is silently DOWNGRADED to a completion gate. It still refuses — E440, *'1 of 2 items are not complete'* — but it is **asking a different question**. Fail-WRONG, and dangerous precisely because it looks like it is working.<br>- **BARE DOCUMENT address**: E441 at EVERY departure, *'address must contain exactly one # file/interior boundary'*. The node becomes UN-DEPARTABLE except by `--force`. Fail-CLOSED and uninformative.<br>- **A correct check link with the same bare address**: E440 quoting the validator's own finding.<br><br>So the bare address is RIGHT for a check gate and WRONG for a completion gate, which is exactly why dropping `check` breaks it. **The severity argument gets WORSE rather than better: silent substitution of INTENT, not silent omission of PROTECTION.** A reader who trusts the gate is being protected against the wrong thing, with a green-looking refusal to reassure them. **The near-miss spelling is still the dangerous one, and it is still the one that passes.** Relatedly, `plan-complete` is a LIFECYCLE HOOK name, not a check kind; the vocabulary is frozen at `plan-validate` (flow-events.ts:251). That conflation was in this row, not in the doctrine. **THE CORRUPTION IS NOW CONFIRMED IN A LIVE ARTIFACT, which this row previously only INFERRED.** ox found TWELVE nodes in its own flow carrying junk top-level keys written by the malformed doc shape — `{path:'label', value:'P1: Drawer surface'}` on phase-1, `{path:'command', value:'/builder 6 implement'}` on phase-2, and so on — with phase-2 additionally carrying a whole `node` key from an upsert attempt. Exactly as diagnosed: no field allowlist, junk written, ok returned. **Its own 'silent no-op' framing is withdrawn by its author**: it wrote, just never the intended key. **WHAT THIS ROW COST AND WHAT IT BOUGHT.** It asserted a mechanism from a single malformed probe and marked it confirmed, and the owner had to reproduce to take it apart — but the reproduction found a worse defect than the one reported, plus two the reporter never saw, and then a differential re-probe overturned the owner's own caution in our favour. **The lesson is not 'report less confidently'. It is that a report's OBSERVATION and its DIAGNOSIS are separate claims with separate evidence, and this schema gave them one label.** **AND THE ORIGINAL FINDING SURVIVES INTACT, sharper than before**: a confident success receipt beat a visible contradiction in the same terminal — ox read the OLD label in its own render for hours because the write had returned ok. What changed is that the tool was not lying about having written; it wrote the wrong thing, having been told to by us. |
| fr-0012 | A `dd_link` CANNOT BE CLEARED — so the expander's documented job is impossible, and every multiphase flow carries the whole-plan gate on two nodes | blocking | pij-disturbing-ox (found while proving the fr-0011 repair) | chainglass, plan 092 — a live multiphase flow, not a fixture | Setting a `dd_link` works. **Clearing one is impossible by every available route.** `apply {"op":"set","id":"review-1","dd_link":null}` → error, *'needs a non-empty string address'*. `set-node` → no `--dd-link` flag at all. **This lands directly on the documented doctrine.** The expander's stated job (flight-plan-ops.md §3c, 'The expander MOVES the check gate') is to move the whole-plan `plan-validate` check from `review-1` to the new last review when phases 2..N are spliced — and the doctrine gives the reason itself: a `plan-validate` gate left on `review-1` of a multi-phase flight *'would refuse a departure that is legitimately mid-plan'*. **Moving requires clearing. Setting the new one works; clearing the old one cannot be done.** So every multiphase flow ends up with the whole-plan gate on TWO nodes, by construction. ox's flow is in that state now: review-1 carries a check that should have moved to review-3, which also has it.<br><br>**THE MECHANISM, from meadowlark's own reproduction, and it is worse than a missing feature: `fieldsFrom` ALREADY HAS THE CLEAR PATH — delete-on-null — annotated as unreachable, because `parseOp` refuses null as malformed before it can ever run.** The capability was written, and it is fenced off by a validator that cannot tell MALFORMED from EXPLICITLY-CLEARED. So the fix is not to build clearing; it is to stop the guard eating the one input that means it. | None through the flow CLI. The only escape from the stale review-1 gate is `--force`, which is the human's and records a `dd-gate-override` event — i.e. the documented remedy for a defect is an override an agent is forbidden to use on its own judgment. | Make `dd_link` clearable — accept an explicit null/unset, and expose `--dd-link` (with a clear form) on `set-node`/`insert-node`.<br><br>**TRACKED UPSTREAM: AI-Substrate/harness-engineering#137 — ITS OWN ISSUE, explicitly not a checklist row on #135.** meadowlark reproduced all three clear forms itself before filing (all refused E108, field survives) and carried the quieter-not-more-correct reasoning into the issue body so the separation survives it. Its coder is fenced off #137 and briefed to design the `--dd-link` flag knowing clearing is part of the contract. | confirmed | NOT independently hit here, and the reason is worth recording rather than leaving as a blank: **this repo has never run the expander**. Plan 001's phases were template-baked at `plan new`, so we never spliced phases 2..N and never needed to move a gate. We are immune for the same accidental reason as fr-0009 — not by design, but because we never exercised the path.<br><br>**BUT THE DEFECT CLASS WAS PROBED AGAINST DD ITSELF RATHER THAN ASSUMED ABSENT, AND THE FIRST ANSWER WAS WRONG.** In a scratch corpus: `dd set '&lt;addr&gt;/note' null --value-json` → **E451, 'value must be a string', field survives** — which looks exactly like the upstream defect and was recorded as such for about a minute. It is not. `dd rm '&lt;addr&gt;/note'` → **ok, and the row comes back `{id: 'r-1'}`.** dd's `rm` is documented as removing 'the item, entry, field or section a dd address names', and it does. **dd does not have this defect, and the reason it does not is the design lesson: CLEARING HAS ITS OWN VERB.** dd never asks a setter to distinguish null-as-clear from null-as-malformed, so it never needs a guard that can get that distinction wrong. Sent to meadowlark as input to #137's flag design. | **THIS IS THE SHARPER DEFECT AND IT WAS FOUND BY PROVING THE OTHER ONE.** fr-0011 says the documented example is malformed — bad, and fixable by editing a doc. This says that even written CORRECTLY, the documented operation cannot be performed. **A fix to fr-0011 would have made the expander fail more quietly, not more correctly**, and would plausibly have closed with a green test while every multiphase spine still carried two whole-plan gates. **The pattern, and it is the one worth carrying**: fr-0011 was found by someone following the doctrine and being betrayed by it; fr-0012 was found by someone VERIFYING THE REPAIR rather than accepting that the repair existed. The second is the step that is usually skipped, and it is the one that found the defect the first fix would have hidden. Note also who found what: the flow's OWNER established the mechanism, and the CONSUMER — with no authority over it and a real incomplete target the owner did not have — ran the conclusive test and then found this. Neither seat could have produced both halves.<br><br>**AND THE CHECK ALMOST DID NOT HAPPEN.** The probe above initially 'confirmed' dd had the same defect, on the strength of `set null` being refused. That was one command, and the conclusion was drawn before asking whether another verb covered it — the same single-probe error that started fr-0011, committed while writing up fr-0011's lesson. `dd rm` was one command away and reversed it. **The general form is the one worth keeping: a probe that finds the expected failure is the one least likely to be followed up, because it agrees with you.** |
| fr-0013 | `harness plan new` scaffolds 6 of 22 schema-declared sections, and no writer verb can create the other 16 | blocking | pij-alleged-junglefowl (PM, dd tally-columns) | dd worktree feat/tally-columns, running /builder 1b plan dd-natively as instructed | `harness plan new 003-tally-columns --title … --phase …` created plan.dd.json with exactly SIX sections: meta, summary, goals, non_goals, acceptance_criteria, phases. The builder/plan schema declares TWENTY-TWO. The sixteen missing include the ones the plan verb's own contract tells the agent to write: gate_matrix (the G1–G7 verdicts), key_findings, clarifications, open_questions, planning_seam, workshop_opportunities, research_context, testing_strategy, documentation_strategy, risks, acceptance_coverage, execution_guardrails. Writing to any of them is refused: `harness dd set …#research_context` and `harness dd add …#gate_matrix` both answer E450 "the document has no section". There is no verb anywhere in the surface that creates a section — `dd` has get/set/add/rm over section INTERIORS only. So the paved path cannot express the artifact the paved path asks for. | None that respects the rules. The plan skill says explicitly `never hand-edit plan.dd.json`, and the only way to birth a section is to hand-edit it. I chose the honest degradation instead: I wrote everything the six available sections could carry, put the phase-gating rationale into phases[].brief/key_risks/note where it has a natural home, and recorded the gate verdicts nowhere — because there is nowhere. `harness plan validate` returns 0 errors on the six-section document, so the missing sections are genuinely optional to the SCHEMA; it is the plan VERB's contract they are mandatory to. | Either `harness plan new` scaffolds every section the schema declares (empty is fine — an empty section is writable, a missing one is not), or dd grows a way to add a declared section to an existing document. The first is smaller and needs no new verb. Note the asymmetry that makes this sharp: an optional section is indistinguishable from a forgotten one, and the writer surface treats both as unwritable. | confirmed | — | OWNERSHIP SPLIT (koala triage, 2026-08-12). THEIR half: `harness plan new` scaffolding only 6 of 22 declared sections — fixed, unmerged, in PR #154. OUR half, and it is the load-bearing one: **no verb anywhere can create a section in an existing document** — `dd add`/`dd set` both answer E450 "the document has no section", and dd's writer surface addresses section INTERIORS only. That stays open and is now ours to design.<br><br>It is adjacent to the schema-editing-verbs workshop but is NOT the same gap and must not be folded into it: the workshop is about writing SCHEMAS (a capability dd has never had, gated on Q1). This is about writing a DOCUMENT — a surface dd already owns, with staging, rollback and sibling lockstep already built — that simply has no verb for one structural operation. A document-verb gap, not schema CRUD.<br><br>The sharp part survives the split: `harness plan validate` returns 0 errors on a document missing two thirds of its contract, so nothing detects the absence. |
| fr-0014 | `harness dd add` refuses a bare string where `dd set` coerces one — and the documented example uses the bare string | annoying | pij-alleged-junglefowl (PM, dd tally-columns) | dd worktree feat/tally-columns, filling plan.dd.json#goals | `harness dd add "plan.dd.json#goals" "An agent can discover the feature from the docs alone."` is refused with `the item to add is not valid JSON` (E453). The `goals` section is `array of text`, so the item's declared type is a string. `dd set` on the same document coerces a bare argument to the declared type — that is what `coerceValue` exists for — but `add` JSON.parses unconditionally. The two writer verbs disagree about whether a declared type is honoured. The builder skill's own worked example is `harness dd add "${PLAN_PATH}#goals" "&lt;goal&gt;"`, i.e. the refused form. | JSON-encode every scalar before adding: `dd add …#goals '"An agent can…"'`. Fine once you know; the error message does not hint at it, and the documented example actively teaches the wrong shape. | Make `add` coerce through the declared item shape exactly as `set` does, falling back to JSON only when the shape is object/array. If that is deliberate, the message should say so — `an array of text needs a JSON string: '"…"'` — rather than the generic invalid-JSON line. | confirmed | — | OWNERSHIP SPLIT (koala triage, 2026-08-12). THEIRS: the builder skill's worked example teaches the refused bare-string form. OURS: the coercion asymmetry itself — `set` coerces through the declared item shape and `add` JSON.parses unconditionally — is a dd behaviour and ours to RULE, not merely to fix. Both readings are defensible: `add` takes an ITEM and an item of an object array genuinely is JSON, so demanding JSON is coherent; but then `set` on the same cell coercing a bare string is the inconsistency, and a caller cannot predict which verb honours the schema. Whichever way it is ruled, the two verbs should answer the same question the same way, and the error should name the expected form. |
| fr-0015 | `harness plan new` has no ordinal, so it silently creates a second, competing plan folder | degrading | pij-alleged-junglefowl (PM, dd tally-columns) | dd worktree feat/tally-columns, scaffolding into an existing docs/plans/003-tally-columns/ | The SDD convention — and every folder already in docs/plans/ — is `&lt;ordinal&gt;-&lt;slug&gt;`. `harness plan new` names the folder after the bare slug with no `--ordinal` flag and no awareness of an existing ordinal folder for the same slug, so `harness plan new tally-columns` created `docs/plans/tally-columns/` beside the live `docs/plans/003-tally-columns/`. Nothing warned. Two plan folders for one plan is precisely the state the ordinal convention exists to prevent, and the flight plan's dd_link gate addresses are anchored to the ordinal path — so the scaffold landed where the gates do not point. | Delete the stray folder and re-run with the ordinal baked into the slug: `harness plan new 003-tally-columns`. That lands the right folder but makes the ordinal part of the plan's slug forever. | Either accept `--ordinal`, or detect an existing `docs/plans/*-&lt;slug&gt;/` and reuse it rather than minting a sibling. Reuse is what the builder skill's own stage modules do ('reuse an existing plan folder before minting one') — the CLI just doesn't. | confirmed | — | OWNERSHIP: theirs (koala triage, 2026-08-12), with a residue they carry — the ordinal-awareness gap in `harness plan new` is theirs to close; the convention it violates is the SDD folder convention, so the residue is that a fix must also decide whether the CLI reuses an existing `docs/plans/*-&lt;slug&gt;/` (what the builder stage modules already do) or merely accepts `--ordinal`. Reuse is the stronger fix: it removes the failure mode rather than adding a flag someone must remember. |
| fr-0016 | `harness flow orient --json` returns strictly less than the human output an agent is told to consume | papercut | pij-alleged-junglefowl (PM, dd tally-columns) | dd worktree feat/tally-columns, the every-turn re-grounding ritual | RETRACTED 2026-08-12 by my own re-check at koala's prompting. NOT a divergence — my reader error, twice over, in the same one-liner.<br><br>Re-run against the same global bin (harness 0.13.0, harness-engineering @ 6d2df71d), `harness flow orient --json` returns data.node.{id,label,command,instructions[]} AND a chores[] array with anchor/status/importance/pip per chore. Everything the human output shows is there.<br><br>My original probe printed `d.now ?? d.node` — and `d.now` is the truthy STRING "phase-2", so the ?? never fell through; I printed a node id and concluded the node object was missing. Then I looked for `due_chores`, which is the field name on `nav show`, not on `orient` (which calls it `chores`). Two wrong reads, one conclusion, filed as a tool defect.<br><br>The residual emptiness I would have seen anyway is also mine: instructions[] is [] on phase-2 because MY expander batch authored none. On a template-seeded node it is populated — `plan` returns command "/builder 1b plan" and 5 instructions. | Call `orient --no-json` and read the prose, or call `nav show --json` separately for `due_chores`. Two calls where the contract implies one. | Put the node object — label, command, instructions[], due_chores — into the `--json` envelope's data, so an agent reading the machine surface sees what the human surface shows. | not-reproducible | Not independently hit — the opposite: koala's counter-observation on the same global bin is what prompted the re-check that retracted it. | Kept rather than deleted, and kept at full length, because a retracted friction is evidence about the REPORTER's instruments, and this ledger is worth more if that is visible. The lesson generalises past this row: `a ?? b` is not a fallback when `a` is a populated string, and a field name that differs between two verbs of the same CLI is exactly where a reader will assume rather than check. I asserted a tool was missing data I had not actually looked for. |
| fr-0017 | Two dd implementations hardcode their own name into RENDER_BANNER, so every document written by one is permanently drift-flagged by the other | blocking | pij-alleged-junglefowl (PM, dd tally-columns) | dd worktree feat/tally-columns, running the /builder plan stage through harness plan new + harness dd set/add as the stage instructs | CORRECTED 2026-08-12 after a decisive scratch probe — the finding stands, BOTH earlier mechanisms were wrong.<br><br>What I originally claimed: the fork bin writes the .dd.json without regenerating the sibling. FALSE. What koala then hypothesised: the fork renders differently per schema. ALSO FALSE.<br><br>The probe (scratch copies of frictions.dd.json, one row added by each bin, JSON and sibling compared byte-for-byte): the two bins write BYTE-IDENTICAL JSON, and both report sibling_regenerated: true. The rendered siblings differ by EXACTLY ONE LINE — the generated-by banner:<br>  fork  : &lt;!-- GENERATED by `harness dd build` — do not hand-edit; regenerate from the .dd.json. --&gt;<br>  local : &lt;!-- GENERATED by `dd build` — do not hand-edit; regenerate from the .dd.json. --&gt;<br>Everything else, all 52046 bytes of a 17-row ledger, is identical.<br><br>The cause is a hardcoded constant, not a version, a pin, or a schema: `RENDER_BANNER` at src/render/renderer.ts:23-24 is a string literal naming the command that renders it. Each implementation hardcodes its own name. So a document written through `harness dd` carries the fork's banner, and this repo's `dd build --check` re-renders it with OUR banner and reports drift — measured: `local check on fork-written pair: exit=1`.<br><br>That is why `just self-host` went red across four scaffolded task files and the plan: not because anything failed to regenerate, but because every document either bin writes is permanently drifted from the other's view of it, on line 1, forever. | After any `harness plan new` / `harness dd set` / `harness dd add`, re-render with the repo's own bin — `node dist/index.js build &lt;doc&gt;` for every document touched — then re-run `just self-host`. Doing this is what took the gate from red to 810/810 green. | Make the banner not encode which binary rendered it — either a name-free banner, or one derived from the document rather than the invoking command. It is one string constant on each side and it removes an entire class of false drift between the fork and the port. Until then, any repo that writes with `harness dd` and checks with a standalone dd will red its own drift gate on line 1 of every document, with a diff that looks like a rendering bug and is not. | confirmed | koala attempted reproduction from the harness side and could NOT reproduce it on a harness document under a harness schema — correctly, since regeneration was never the mechanism. The decisive probe ran from this side on scratch copies. | Ownership: theirs via PR #154 (koala), with the CLASS staying ours to watch — a shared render surface where each side hardcodes its own identity will diverge again on the next such constant. Worth saying plainly: my first mechanism was wrong and koala's correction was right about the pin story, but the per-schema hypothesis that replaced it was also wrong. Three explanations, one probe, one line of evidence. The finding survived all three because it was reported as an OBSERVATION (self-host went red, four files, here is the command) and not as a theory. |
| fr-0018 | `dd doctor` sweeps GITIGNORED paths, so deliberately-not-repo-content can fail a gate | degrading | pij-alleged-junglefowl (PM, dd tally-columns) | dd main checkout, after placing a demo under scratch/ at Jordan's instruction | `scratch/` is gitignored in this repo (.gitignore:177) — an explicit declaration by the repo that its contents are not repo content. `dd doctor` sweeps it anyway. A demo corpus placed at scratch/tally-demo/ (a schema + a document + its sibling, put there because scratch/ in the main worktree is the only path Jordan's IDE points at) was discovered by the sweep and produced three failures in the local suite — dd doctor / links / graph in test/acts/dd.test.ts, plus the envelope-contract doctor case. CI never saw any of it, because CI checks out tracked content only. So the repo's own gate fails on a machine where someone has put scratch content in the place they were told to put it. | Move the scratch content outside the repo (which defeats the reason it was placed there — an IDE-visible path), or delete it before running the suite. The o-prime hit this and parked the directory while diagnosing, which is the more expensive failure mode: the workaround for a false gate failure was removing a colleague's deliberate artifact. | Honour `.gitignore` in the sweep, or at minimum add `scratch/` to SCAN_SKIP_PATHS. The precedent is already in our own source and carries the reasoning: SCAN_SKIP_PATHS excludes `.harness/temp` for exactly this purpose, citing AC-15 — deliberately-not-repo-content must never shape a gate. A `.gitignore` entry is that same declaration, made by the repo rather than by us, and dd currently ignores it. Honouring `.gitignore` is the general fix; the SCAN_SKIP_PATHS entry is the plaster. | open | — | Filed after the tally merge (5aea4bd) at the o-prime's ruling, and REPRODUCED LIVE at filing time rather than recalled: `dd doctor` in the main worktree answered `error` / E438 — "1 ERROR-class finding(s) across 14 document(s)" — sourced entirely from scratch/tally-demo/, a gitignored path. The specific finding was a schema that could not resolve from the sweep's discovery roots, but the class is the point: nothing under a gitignored path should be able to produce ANY verdict, resolvable or not.<br><br>Note the asymmetry that makes this worth fixing rather than documenting: the sweep is STRICTER than git about what belongs to the repo, so the more carefully a developer separates scratch from source, the more likely they are to fail a gate for it. And the workaround has a cost this repo has already paid — the o-prime hit this and parked the directory while diagnosing, which meant the remedy for a false gate failure was removing a colleague's deliberate artifact.<br><br>The demo has since been rearranged so its schema resolves (doc-folder resolution looks beside the DOCUMENT, not at a demo root), and doctor is back to ok / 0 errors / 0 warns. That fixes the instance and leaves the finding exactly where it was: the sweep still reads gitignored content. |
| fr-0019 | THE CLASS: three verdicts in four days that depended on the developer's disk rather than the repository | degrading | pij-alleged-junglefowl (PM, dd tally-columns), class named at pij-mental-dajeil's request | dd, across three separate incidents 2026-08-09 to 2026-08-13 | Three times in four days, a gate in this repo answered a question about the DEVELOPER'S DISK while appearing to answer a question about the REPOSITORY. The instances differ; the shape is identical.<br><br>  i.  6fc7d83 — `dd schema list` reads ~/.dd, so five schemas in a developer's HOME shadow the repo copies and the envelope-contract test fails for that developer while passing in CI. VERDICT LIVES IN $HOME.<br><br>  ii. 0b87fe4 -&gt; bcae675 — `government/` was relocated to `.harness/government/` and four readers were repointed; `test/docs-surface.test.ts` was not. Every developer whose working tree still carried the old directory saw green while main's CI had been RED for two consecutive commits. VERDICT LIVES IN A STALE DIRECTORY. The mechanism was demonstrated in both directions during the tally rebase: the directory survived on my disk only because my branch predated the move, and checking out a base that DELETES it is what finally made my disk agree with CI. The test was never the variable; the disk was.<br><br>  iii. fr-0018 — `dd doctor` sweeps gitignored paths, so a developer with local scratch content fails a gate that CI cannot see. VERDICT LIVES IN UNTRACKED WORKING-TREE CONTENT.<br><br>What makes the class worth more than any instance: THE DIRECTION ALTERNATES. (i) and (iii) are red locally and green in CI. (ii) was green locally and red in CI — and that one shipped two red commits to main before anyone noticed, because the developers who could have caught it were the ones whose disks were lying to them. Both directions of the error have now happened here. | Per instance: clean HOME for (i), rebase or delete the stale directory for (ii), remove scratch content for (iii). There is no workaround for the class — the point of the class is that the next instance will look different and be the same. | A standing property for gates in this repo: a gate's verdict should be a function of tracked repository content and nothing else. Anything a gate reads that is not tracked — $HOME, an untracked directory, a gitignored path, ambient environment — is an input that varies per machine and therefore cannot support a claim about the repository. Where a gate genuinely must read outside the tree, it should say so in its failure message, so a developer knows whether they are looking at a repo defect or a local one. That message alone would have saved most of the time lost to all three instances. | open | — | A test whose answer depends on the disk it runs on is not a gate — it is a coin whose bias varies by machine. Filed as the class at the o-prime's instruction, with all three as instances, because each was individually dismissible as bad luck and together they are a property of how gates are written here. Two of the three were found not by reading code but by a verdict disagreeing between two machines, which is the only detector this class currently has. |
