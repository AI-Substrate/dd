# Pending governance work

**Writer**: `pij-mental-dajeil` (o-prime), single-writer. Drain and delete rows as they land.

This file exists because the o-prime has twice lost a thing it intended to do, and
"remember to apply it later" is the discipline-shaped fix this repo keeps ruling against.
A row here is durable and citable; an intention is not.

---

**Nothing pending.** All seven plan-001 rows drained 2026-08-07. The file is kept rather
than deleted because the next hold needs somewhere to go, and re-creating it is one more
thing to remember.

## Drained — where each landed

| Row | Landed in |
|---|---|
| P-1 derive the commit message, don't compose it | `plan.dd.json#execution_guardrails` — its own row |
| P-2 worktree-per-writer | `justfile` (`just worktree` / `just worktrees`) + `orient-local.md` Repo mechanics |
| P-3 a decaying figure can weaken an argument | `plan.dd.json#execution_guardrails` — its own row |
| P-4 a correct claim delivered at a cost the sender does not pay | `orient-local.md` § where mechanism cannot reach, item 4 |
| P-5 a clean sweep pre-authorises not looking | same section, item 5 |
| P-6 crediting a local reaction as method | same section, item 6 |
| P-7 the urge to consolidate is the urge to overstate coverage | same section, item 7 |

**P-1 and P-3 were filed here as clauses of the measured-at guardrail, and drained as
separate rows instead** — P-7's test applied on its first use. That rule's action is *stamp
or derive a measurement*; P-1's is *read `git diff --cached` before describing it*; P-3's is
*ask what the argument loses without the figure*. Three actions, three rows. Consolidate
causes, never instruments.

**Draining also produced one defect, caught before commit**: inserting items 4–7 left the
"three known axes" summary stranded mid-list and understating its own count. It now sits
below the list and reads four. Item 3 is the rule that catches this, and item 3 did not
catch it — a reader did, at commit time. Recorded rather than quietly fixed.
